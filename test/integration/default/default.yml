---
- hosts: test-kitchen

  vars:
    java_package: oracle-java8-installer
    elasticsearch_plugins: [ cloud-aws ]
    elasticsearch_cluster_name: kitchenCluster

  pre_tasks:
    - name: Install curl
      apt: name=curl state=present

  roles:
    - wunzeco.java
    - ansible-elasticsearch
